{% extends 'base.html.twig' %}

{% block title %}EnergyOption{% endblock %}

{% block body %}
    <h1>EnergyOption</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ energy_option.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ energy_option.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('energy_option_index') }}">back to list</a>

    <a href="{{ path('energy_option_edit', {'id': energy_option.id}) }}">edit</a>

    {{ include('energy_option/_delete_form.html.twig') }}
{% endblock %}
