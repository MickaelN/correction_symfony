{% extends 'base.html.twig' %}

{% block title %}Hello CarController!
{% endblock %}

{% block body %}
	<div class="example-wrapper">
		<h1>Nos voitures</h1>
		<div class="row">
			<div class="col-4 car">
				<h2>{{ carInfo.brand }}</h2>
				<p>{{carInfo.model}}</p>
				<a href="{{path('carInfo')}}" class="btn btn-success">Voir plus</a>
			</div>
		</div>
	</div>
{% endblock %}
