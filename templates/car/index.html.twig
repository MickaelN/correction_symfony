{% extends 'base.html.twig' %}

{% block title %}Hello CarController!
{% endblock %}

{% block body %}
	<div class="example-wrapper">
		<h1>Nos voitures</h1>
		<div class="row">
			<div class="col-4 text-center car">
				<h2>{{ carInfo.brand }}</h2>
				<p>{{carInfo.model}}</p>
				<p>{{carInfo.year}}</p>
				<p>{{carInfo.engine}}</p>
				<p>{{carInfo.color}}</p>
			</div>
		</div>
	</div>
{% endblock %}
